namespace Geaux.Specification;

public static class SpecificationImports
{
    // Dummy references to force inclusion of extension namespaces
    public static readonly System.Type _ = typeof(SpecificationBuilderExtensions);

}